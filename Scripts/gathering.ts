import { openModal, closeModal } from "./components/modal/index.js";

// get the first week
// check if the first day falls on a certain day of the week
// add a style that adds padding to the first week

openModal("gathModal", "openGM");
closeModal("gathModal", "closeGM");